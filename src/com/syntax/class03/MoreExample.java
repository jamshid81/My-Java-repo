package com.syntax.class03;

public class MoreExample {

	public static void main(String[] args) {
		
		boolean flag=false;
		if (flag) //if(ID) is true by defalt
			//in this example we see that the ID=fasle; therefore, the system will not print
			//"Hello", now if we change the ID=ture (flag=ture) the system will print "Hello"
		{System.out.println("hello");
		
		}else {System.out.println("bye");
		
		}
		System.out.println(flag);
		// the display word "false" is from the line # 16 print



	}

}
